# Análise: .mise.toml vs Diretrizes Padrão

**Data:** 2026-01-22  
**Branch:** demo-nocodb-simple  
**Referências:**
- `estrutura-padrao1-2201/contextos/globais/ambiente/mise/SKILL.md`
- `estrutura-padrao1-2201/contextos/projetos/flusistip/mvp/stack-tecnico/ambiente/`

---

## 📋 Diretrizes Identificadas

### 1. SSOT (Single Source of Truth)
✅ **Status:** CONFORME

`.mise.toml` deve ser a única fonte de verdade para:
- Versões de ferramentas
- Variáveis de ambiente  
- Hooks de automação
- Tasks do projeto

**Implementação atual:** ✅ Correto

---

### 2. Estrutura de [tools]

```toml
[tools]
# Estratégia de versões:
# - Versão fixa: Estabilidade crítica (java = "21.0.2")
# - latest: Ferramentas CLI (trivy = "latest")
# - lts: Compatibilidade (node = "lts")
```

**Nossa implementação:**
```toml
[tools]
bun = "1.3.3"     # ✅ Versão fixa (runtime principal)
node = "24.11.1"  # ✅ Versão fixa (fallback)
python = "3.12"   # ✅ Versão genérica
```

✅ **Status:** CONFORME (app-controle não usa trivy/gitleaks ainda)

---

### 3. Estrutura de [env]

```toml
[env]
PROJECT_NAME = "nome-projeto"
PROJECT_TYPE = "react-vite-bun"
PROJECT_ENV = "development"
VITE_PORT = "3000"
```

**Nossa implementação:**
```toml
[env]
PROJECT_NAME = "ultrathink"
PROJECT_TYPE = "react-vite-bun"
NODE_ENV = "development"
VITE_PORT = "3000"
NOCODB_URL = "http://localhost:8080"  # ✅ Novo
VITE_API_URL = "http://localhost:3000" # ✅ Novo
```

✅ **Status:** CONFORME + Extensões específicas NocoDB

---

### 4. Estrutura de Tasks

**Padrão:**
```toml
[tasks.nome]
description = "Descrição clara e concisa"
run = """
#!/bin/bash
set -e
echo "Mensagem informativa com símbolos"
# Comandos
"""
```

**Nossa implementação:**
✅ Todas as tasks seguem este padrão
✅ Uso de símbolos visuais (✅, ⚠️, 🔒, 📍, etc)
✅ `set -e` em scripts bash
✅ Mensagens informativas

**Status:** ✅ CONFORME

---

### 5. Hook enter (Crítico)

**Padrão FluSisTip:**
```toml
[hooks]
enter = """
# 1. Verificar e instalar deps (idempotente)
# 2. Verificar lockfile atualizado
# 3. Verificar ferramentas instaladas
# 4. Exibir menu de comandos
"""
```

**Nossa implementação:**
❌ **AUSENTE** - Não implementado ainda

**Status:** ⚠️ **NÃO CONFORME** - Necessário adicionar

---

### 6. Hook leave (Opcional)

**Padrão:**
```toml
leave = """
echo "👋 Saindo do projeto..."
# (Opcional) pkill processos
"""
```

**Nossa implementação:**
❌ **AUSENTE**

**Status:** ⚠️ **RECOMENDADO** adicionar

---

### 7. Ferramentas de Segurança

**Padrão Healthcare:**
```toml
[tools]
trivy = "latest"     # Scanner vulnerabilidades
gitleaks = "latest"  # Detector secrets
grype = "latest"     # Scanner SBOM
age = "latest"       # Criptografia local
```

**Nossa implementação:**
❌ **AUSENTE** - app-controle não usa ferramentas de segurança ainda

**Status:** ⚠️ **OPCIONAL** para demo, mas recomendado

---

### 8. Task de Segurança

**Padrão FluSisTip:**
```toml
[tasks.security]
description = "Scan completo de segurança"
run = """
echo '🔒 Security Scan'
gitleaks detect --source . --no-git --redact
trivy fs --severity HIGH,CRITICAL .
clj-kondo --lint src --parallel
"""
```

**Nossa implementação:**
❌ **AUSENTE**

**Status:** ⚠️ **OPCIONAL** para MVP demo

---

### 9. Idempotência

**Diretriz:** Tasks devem ser executáveis múltiplas vezes sem efeitos colaterais

**Exemplos na nossa implementação:**
```bash
# ✅ nocodb:setup - verifica se .env existe
if [ ! -f .env.nocodb ]; then
    cp .env.nocodb.example .env.nocodb
fi

# ✅ nocodb:health - apenas leitura
# ✅ check - apenas leitura
# ⚠️ nocodb:reset - destrutivo (mas tem confirmação)
```

✅ **Status:** CONFORME

---

### 10. Verificações Pré-Execução

**Diretriz:** Verificar condições antes de executar ações

**Exemplos:**
```bash
# ✅ e2e:ui - verifica se serviços estão rodando
if ! curl -s http://localhost:3000 > /dev/null 2>&1; then
    echo "⚠️  Dev server não está rodando!"
    echo "💡 Execute em outro terminal: mise dev"
    exit 1
fi
```

✅ **Status:** CONFORME

---

### 11. Mensagens Informativas

**Diretriz:** Usar símbolos visuais e mensagens claras

**Símbolos usados:**
- ✅ Sucesso
- ❌ Erro
- ⚠️ Aviso
- 💡 Dica
- 🔍 Verificando
- 📦 Instalação
- 🐳 Docker
- 🎭 Testes
- 🔒 Segurança

✅ **Status:** CONFORME

---

## 📊 Resumo Comparativo

| Diretriz | Status | Prioridade | Ação |
|----------|--------|------------|------|
| **SSOT** | ✅ CONFORME | Alta | - |
| **[tools]** | ✅ CONFORME | Alta | - |
| **[env]** | ✅ CONFORME | Alta | - |
| **Tasks estruturadas** | ✅ CONFORME | Alta | - |
| **Hook enter** | ❌ AUSENTE | **ALTA** | **Adicionar** |
| **Hook leave** | ❌ AUSENTE | Média | Adicionar |
| **Ferramentas segurança** | ❌ AUSENTE | Baixa (MVP) | Opcional |
| **Task security** | ❌ AUSENTE | Baixa (MVP) | Opcional |
| **Idempotência** | ✅ CONFORME | Alta | - |
| **Verificações pré-exec** | ✅ CONFORME | Alta | - |
| **Mensagens informativas** | ✅ CONFORME | Alta | - |

---

## 🎯 Adequações Necessárias

### Prioridade ALTA (Obrigatório)

#### 1. Adicionar Hook enter

**Funcionalidades:**
- Verificar se node_modules existe (idempotente)
- Verificar se NocoDB está configurado
- Verificar se .env.nocodb existe
- Exibir menu de comandos

**Código proposto:**
```toml
[hooks]
enter = """
# ────────────────────────────────────────────────────────────────
# app-controle - Ambiente Automático (demo-nocodb-simple)
# ────────────────────────────────────────────────────────────────

# 1. Verificar e instalar deps (idempotente)
if [ -f "package.json" ] && [ ! -d "node_modules" ]; then
  echo "📦 Instalando dependências (bun)..."
  bun install --silent && echo "   ✓ Dependências instaladas"
fi

# 2. Verificar .env.nocodb
if [ ! -f ".env.nocodb" ]; then
  echo "⚠️  Arquivo .env.nocodb não encontrado"
  echo "   💡 Execute: mise nocodb:setup"
fi

# 3. Verificar Docker
if ! command -v docker &> /dev/null; then
  echo "⚠️  Docker não encontrado (necessário para NocoDB)"
  echo "   💡 Instale Docker: https://docs.docker.com/get-docker/"
fi

# 4. Menu de comandos
echo ""
echo "╔════════════════════════════════════════════════════════════════╗"
echo "║  📦 app-controle - MVP Simples (Bash + NocoDB)                 ║"
echo "╠════════════════════════════════════════════════════════════════╣"
echo "║  Frontend:  mise dev              (React + Vite)               ║"
echo "║  Backend:   mise nocodb:start     (PostgreSQL + NocoDB)        ║"
echo "║  Full:      mise full-stack       (Frontend + Backend)         ║"
echo "║  E2E:       mise e2e:ui            (Playwright UI)             ║"
echo "║  Verify:    mise check             (Verificar ambiente)        ║"
echo "║  Help:      mise help              (Lista todos comandos)      ║"
echo "╚════════════════════════════════════════════════════════════════╝"
echo ""
"""
```

---

### Prioridade MÉDIA (Recomendado)

#### 2. Adicionar Hook leave

```toml
leave = """
# Cleanup suave (sem forçar kill de processos)
echo "👋 Saindo do app-controle..."
# Lembrete útil
if docker ps | grep -q app-controle-nocodb; then
  echo "💡 NocoDB ainda está rodando. Para parar: mise nocodb:stop"
fi
"""
```

---

### Prioridade BAIXA (Opcional para MVP)

#### 3. Adicionar Ferramentas de Segurança

**Apenas se o projeto evoluir para produção:**

```toml
[tools]
# Segurança (opcional para produção)
# trivy = "latest"     # Scanner vulnerabilidades
# gitleaks = "latest"  # Detector secrets
# grype = "latest"     # Scanner SBOM
```

#### 4. Adicionar Task security

```toml
[tasks.security]
description = "🔒 Scan de segurança (opcional - requer trivy + gitleaks)"
run = """
#!/bin/bash
echo "🔒 Security Scan"
echo "═══════════════════════════════════════════════════════════════"

# Verificar se ferramentas estão instaladas
if ! command -v gitleaks &> /dev/null; then
  echo "⚠️  gitleaks não instalado. Execute: mise use gitleaks"
  exit 1
fi

if ! command -v trivy &> /dev/null; then
  echo "⚠️  trivy não instalado. Execute: mise use trivy"
  exit 1
fi

# 1. Gitleaks
echo "📍 [1/3] Gitleaks - Verificando secrets..."
gitleaks detect --source . --no-git --redact 2>/dev/null

# 2. Trivy
echo "📍 [2/3] Trivy - Verificando vulnerabilidades..."
trivy fs --severity HIGH,CRITICAL --skip-dirs node_modules,dist .

# 3. ESLint
echo "📍 [3/3] ESLint - Lint JavaScript..."
bun run lint

echo "═══════════════════════════════════════════════════════════════"
echo "✅ Security scan concluído!"
"""
```

---

## 🔄 Diferenças Justificadas

### 1. Foco em NocoDB vs Clojure

**FluSisTip (referência):**
- Backend: Clojure + Datomic
- Tasks: repl, lint (clj-kondo), security (trivy + gitleaks)

**app-controle (nossa implementação):**
- Backend: NocoDB + PostgreSQL (dashboard visual)
- Tasks: nocodb:*, e2e:*, full-stack

✅ **Justificativa:** Arquiteturas diferentes, diretrizes gerais aplicam-se

---

### 2. Sem Ferramentas de Segurança

**FluSisTip:** Healthcare compliance (LGPD, HIPAA) requer trivy + gitleaks

**app-controle:** MVP demo educacional (não processa dados sensíveis)

✅ **Justificativa:** Segurança proporcional ao risco. Para produção, adicionar.

---

### 3. Tasks Específicas de Testes E2E

**Nossa adição:** `e2e:ui`, `e2e:headless`, `e2e:install`

**Justificativa:** NocoDB é UI-first, testes E2E são críticos para validar dashboard

✅ **Status:** Extensão válida das diretrizes

---

## ✅ Recomendações Finais

### Implementar AGORA (Prioridade ALTA)

1. ✅ **Hook enter** - Essencial para onboarding zero-friction
2. ✅ **Hook leave** - Boas práticas

### Implementar DEPOIS (Futuro)

3. ⚠️ **Ferramentas segurança** - Se evoluir para produção
4. ⚠️ **Task security** - Se evoluir para produção

---

## 📝 Checklist de Conformidade

- [x] SSOT centralizado em .mise.toml
- [x] Estratégia de versões apropriada
- [x] Variáveis de ambiente PROJECT_*
- [x] Tasks com description + run
- [x] Idempotência garantida
- [x] Verificações pré-execução
- [x] Mensagens informativas com símbolos
- [ ] **Hook enter implementado** ← PENDENTE
- [ ] **Hook leave implementado** ← PENDENTE
- [ ] Ferramentas segurança (opcional para MVP)
- [ ] Task security (opcional para MVP)

**Score:** 8/11 (73%) → **Adicionar hooks = 10/11 (91%)**

---

## 🎯 Conclusão

**Status geral:** ⚠️ **73% CONFORME** → ✅ **91% com hooks**

**Ação imediata:** Adicionar hooks enter e leave para atingir conformidade total com diretrizes de ambiente automatizado.

**Versão recomendada:** `.mise.toml.v2` (com hooks)

---

**Análise por:** Droid  
**Data:** 2026-01-22  
**Referências:** estrutura-padrao1-2201/contextos/globais/ambiente/mise/
